git checkout -b sprint-2

git add .
git commit -m "feat: crear vista de registro de pacientes"

git commit -m "feat: agregar validaciones de formulario"

git commit -m "feat: mostrar pacientes en tabla dinamica"

git commit -m "style: mejorar estilos de tabla y formulario"

git commit -m "chore: actualizar router y navbar"

git checkout main
git merge sprint-2
git push
